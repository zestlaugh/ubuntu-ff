name: ubunturdp

on:
  workflow_dispatch:
    inputs:
      auth:
        description: 'GRDP Authorization Code'
        required: true
        default: 'paste your code here'

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Creating User to Login
      run: |
        sudo useradd -m ash
        echo 'ash:ash' | sudo chpasswd
        sudo usermod -aG sudo ash

    - name: Installing Desktop Environment (wait for 10 min)
      run: |
        sudo apt update
        sudo apt install -y expect ubuntu-desktop

    - name: Installing Google Chrome Headless + Tools
      run: |
        set -e

        echo "[+] Creating Ubuntu user"
        sudo useradd -m Ubuntu
        echo 'Ubuntu:1234' | sudo chpasswd
        sudo usermod -aG sudo Ubuntu
        sudo usermod -aG chrome-remote-desktop Ubuntu

        echo "[+] Switching shell to bash"
        sudo sed -i 's|/bin/sh|/bin/bash|g' /etc/passwd

        echo "[+] Installing desktop environment and tools"
        sudo apt-get update
        sudo apt-get install -y xfce4 xfce4-terminal desktop-base xscreensaver \
          nautilus nano python3-pip xvfb

        echo "[+] Disabling lightdm"
        sudo systemctl disable lightdm.service

        echo "[+] Installing Chrome Remote Desktop"
        wget https://dl.google.com/linux/direct/chrome-remote-desktop_current_amd64.deb
        sudo dpkg -i chrome-remote-desktop_current_amd64.deb || sudo apt -f install -y

        echo '[+] Setting up CRD session'
        echo "exec /etc/X11/Xsession /usr/bin/xfce4-session" | sudo tee /etc/chrome-remote-desktop-session

        echo "[+] Installing Google Chrome"
        wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
        sudo dpkg -i google-chrome-stable_current_amd64.deb || sudo apt -f install -y

        echo "[+] Installing Python packages"
        pip3 install nbformat gdown pika

        echo "[+] Installing VS Code"
        wget -O code.deb "https://code.visualstudio.com/sha/download?build=stable&os=linux-deb-x64"
        sudo dpkg -i code.deb || sudo apt -f install -y

        echo "[+] Installing NVM"
        wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash

    - name: Generate GRDP Setup Script
      run: |
        echo "${{ github.event.inputs.auth }}" > setup.sh
        chmod +x setup.sh

    - name: Initialize GRDP
      run: |
        expect -c '
        spawn ./setup.sh
        expect "Enter a PIN of at least six digits: "
        send "123456\r"
        expect "Enter the same PIN again: "
        send "123456\r"
        expect eof
        '

    - name: SSH tunnel over ngrok
      uses: joshlarsen/ssh-tunnel-action@v1.2
